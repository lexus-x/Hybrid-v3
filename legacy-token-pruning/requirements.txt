# Core
torch>=2.6
torchvision>=0.21
timm>=1.0.9
numpy>=1.26
tqdm>=4.66

# Benchmarking (FLOP counting)
fvcore>=0.1.5

# Figures & video
matplotlib>=3.8
imageio>=2.30
# The explainer video also requires a system `ffmpeg` binary on PATH.
